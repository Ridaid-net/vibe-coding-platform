import { PrensaContenido } from '@/components/rodaid/prensa-contenido'

export const metadata = {
  title: 'Portal de Prensa — RODAID',
  description: 'Comunicados oficiales y novedades de RODAID para medios y prensa.',
}

export default function PrensaPage() {
  return <PrensaContenido />
}
