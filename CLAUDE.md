# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo actually is

The `package.json` name (`vibe-coding-agent`) and `README.md` describe a generic Vercel "AI sandbox" template. That's the repo's origin, not its current product. The live application built here is **RODAID**: a bicycle registration / anti-theft civic-tech platform for Mendoza, Argentina (`rodaid.net`), used by citizens, partner workshops ("aliados"), and government entities (Ministerio de Seguridad, MPF Mendoza, several municipalities) as a multi-tenant system.

The original AI-sandbox scaffold (`ai/`, `app/api/chat`, `app/api/sandboxes`, `app/chat.tsx`, `app/file-explorer.tsx`, `app/preview.tsx`, `app/logs.tsx`, `app/state.ts`, `app/actions.ts`, and the `components/chat`, `components/ai-elements`, `components/panels`, `components/commands-logs`, `components/error-monitor`, `components/file-explorer`, `components/model-selector`, `components/tabs` UI) is still present in the tree but is **not wired into the live product** — `app/page.tsx` (the real homepage) only renders `components/rodaid/*`. Don't assume changes to the sandbox/chat scaffold affect anything a user sees; conversely, don't try to bolt new RODAID features onto it.

Deployment is **Netlify**, not Vercel, despite leftover Vercel-flavored deps (`@vercel/sandbox`) and README copy: see `netlify.toml`, `netlify/functions/*.mts` (scheduled/background jobs), and `netlify/edge-functions/auth-admin.ts`.

Code, identifiers, comments, and DB columns are predominantly in **Spanish** — match that convention in new code.

## Commands

Package manager is **pnpm** (`pnpm-lock.yaml`, `engines.node: 22.x`).

```bash
pnpm install
pnpm dev          # next dev --turbopack
pnpm build        # next build
pnpm start        # next start
pnpm lint         # next lint
pnpm type-check   # tsc --noEmit
```

There is no test framework configured (no jest/vitest, no `*.test.*` files, no CI workflow). Verify changes with `pnpm type-check` + `pnpm lint` plus manually exercising the affected flow — many integrations have a documented DEV/STUB fallback (see below) that makes this possible without real credentials.

## Architecture

### Layering convention

- `app/api/v1/**` — the versioned, current API surface. A typical route: authenticate with `requireUser`/`requireRole`, delegate to a `src/services/*.service.ts` function, and on failure return `jsonError(error)`.
- `app/api/**` (non-`v1`) — older or special-purpose endpoints (chat/gpt/faq/legal assistants, sandboxes, admin bootstrap) that predate or sit outside the `v1` convention.
- `src/services/*.service.ts` — business logic and external integrations (MercadoPago, blockchain/BFA anchoring, PDF/PKCS7/PKCS12 signing, IoT, Strava/Garmin, notifications, etc).
- `lib/*.ts` — shared helpers. Note `lib/marketplace.ts` is the de facto cross-cutting kernel despite its name: it defines `getPool()` (the Postgres pool), the `ApiError` class, `jsonError()`, and is where many unrelated routes import auth/db primitives from — check it first when tracing how a route gets its DB client or error shape.
- `lib/auth.ts` / `lib/session.ts` — JWT-based session and role model.
- `lib/tenant.ts` — multi-tenant resolution and RLS-scoped query helpers (`withTenant`, `withBypassRLS`, `auditTenant`). Tenants are resolved by slug (`rodaid`, `ministerio_seguridad`, `mpf_mendoza`, `municipio_*`) to a UUID, then every query in that scope runs inside a transaction with `SET LOCAL app.current_tenant_id` for Postgres RLS isolation. Government-facing APIs pick the tenant from the `X-Tenant-ID` header (`getTenantFromHeader`).

### Data layer

No ORM — raw SQL via `pg`, database provisioned through `@netlify/database` (Neon). Schema lives entirely in sequentially timestamped SQL files under `netlify/database/migrations/`; read recent migrations to understand current table shape rather than assuming `src/types/database.ts` is exhaustive.

### Migrations: a new enum value and its first use need separate deploys

Postgres refuses to use a value added by `ALTER TYPE ... ADD VALUE` until that addition has committed (`unsafe use of new value ... (55P04)`). Splitting the `ADD VALUE` and its first use into two separate **migration files** is not enough by itself — Netlify's migration runner does not commit between files within the same deploy, only between separate deploys. A migration that adds an enum value and a later migration that uses it must land in two different deploys, not just two different files of the same deploy/PR.

This was confirmed empirically (2026-07-08) while building the CIT Completo marketplace/escrow state machine: `20260708000003_extend_marketplace_escrow_cit_completo.sql` (`ALTER TYPE marketplace_publicacion_estado ADD VALUE 'PUBLICADO_PENDIENTE_CERTIFICACION'`, ...) and `20260708000004_backfill_marketplace_estado_cit_completo.sql` (an `UPDATE` that sets rows to that same new value) were pushed together in one PR/deploy and failed with exactly that `55P04` error. Splitting them across two separate deploys on the same branch — deploy `6a4eb7750949730008700e93` (only `...003`, state `ready`) followed by a second, later deploy `6a4eb8ca70cab3000845f0b2` (`...004` alone, state `ready`) — confirmed the fix: same two files, same SQL, no code change, just delivered as two deploys instead of one.

Practical rule: when a migration adds an enum value that a later migration will read/write/filter on, merge the `ADD VALUE` migration to `main` on its own, confirm its production deploy is `ready`, and only then merge the migration that uses the new value.

**Two separate migration files are not the mechanism — two separate deploys are.** It's easy to do the file-splitting correctly and still ship the bug by putting both files in the same commit/PR, which still lands them in one deploy together. This happened a second time (2026-07-09, PR #32, building the Fase 6 escrow states): `20260709000001_escrow_estados_reserva_cit_completo.sql` (`ALTER TYPE escrow_transaccion_estado ADD VALUE 'RESERVA_PENDIENTE'`, ...) and `20260709000002_reindex_escrow_reserva_cit_completo.sql` (an index predicate referencing that same value) were correctly split into two files, but both were committed and pushed together — the PR's deploy failed with the identical `55P04` error (`unsafe use of new value "RESERVA_PENDIENTE"`). Before merging any migration PR, check whether it contains an `ADD VALUE` alongside anything that references the new value — if so, split the PR itself in two, not just the files inside it.

### Known gap: the CIT Completo 20-point checklist isn't persisted yet

`components/rodaid/ChecklistCIT.tsx` (built on `lib/puntos-inspeccion.ts`'s `PUNTOS_INSPECCION`/`ChecklistInspeccion`/`calcularResultadoChecklist`) is a fully-built UI for the 20-point inspection checklist, but it's imported and never rendered in `components/rodaid/inspecciones.tsx` — no `<ChecklistCIT>` JSX usage exists anywhere. The approval flow that's actually wired (`aprobarInspeccion()` client helper → `POST /api/v1/inspecciones/[citId]/aprobar` / `POST /api/inspector/cit` → `aprobarInspeccionFisica()` in `inspeccion.service.ts`) only sends/persists a single verdict (`APROBADA`/`DISCREPANCIA`) plus free-text notes, signed as one acta — never point-by-point results. Treat `resultado = 'APROBADA'` as the real, working seal event (atomic, signed, accelerates the CIT pipeline, and — as of Fase 4 — seals a waiting Marketplace listing) — don't assume per-point checklist data exists anywhere in the DB until this gap is closed in its own pass.

### Known gap: no persistent "resume later" screen for a pending denuncia payment

`components/rodaid/denuncia-mpf-modal.tsx` (Fase 7, caso 2 — dueño sin CIT activo denunciando su propia bici): once the backend returns `estado === 'PENDIENTE_PAGO'`, the modal's only clear exit is "Pagar ahora" (redirects to MercadoPago) plus a small WhatsApp-support link — there's deliberately no "pagar más tarde" button, because there's nowhere for that button to lead. `GET /api/v1/bicicletas/[id]/denuncia` already returns a `pagoPendiente` field with everything needed to resume (`preferenceId`, `initPoint`, `montoARS`), but no frontend component calls this endpoint today — confirmed via a full-repo grep, zero matches outside `denuncia-mpf-modal.tsx`'s own POST call and the backend service/route themselves. If a user closes the modal without paying (still possible via the Dialog's default X/ESC/overlay-click dismiss — that's not blocked), there is currently no page or banner anywhere that lets them come back and finish paying. Fixing this properly is part of the same missing "buyer-side persistent state" screens (a "mis compras"/"mis reservas" view) that Fase 6's `/reservar` and `/confirmar-pago` also need — not something to bolt on as an isolated banner in `garaje-digital.tsx`.

### Pending frontend work: buyer-side purchase UI, denuncia-tercero owner banner, and the outdated single-CIT pricing page (2026-07-10 audit)

A frontend audit (2026-07-10) mapped everything the Fase 3-7 backend work (CIT Completo marketplace/escrow states, the denuncia-de-robo tariff system) still needs on the UI side. Three items from that audit were fixed immediately: `mis-publicaciones.tsx`'s missing `marketplace_publicaciones`/`escrow_transacciones` state labels, `denuncia-mpf-modal.tsx`'s missing `PENDIENTE_PAGO` payment step (see the gap noted above about "resume later" still being unsolved), and `denuncia-comunitaria.tsx`'s stale hardcoded price. Item 8 (below) was fixed later the same day. Two larger items remain, deliberately deferred to their own session:

- **Item 4 — the buyer-side CIT Completo purchase path, re-audited 2026-07-11: the gap was bigger than originally documented, three layers deep.** A deeper investigation (prompted by "let's talk about item 4") found the buyer flow was broken at every layer, not just missing a post-purchase screen: (1) **discoverability** — the live homepage marketplace grid (`components/rodaid/marketplace.tsx`, called from `app/page.tsx`) never passed an `estado` filter, and the backend (`app/api/v1/marketplace/route.ts`) defaulted to `estado = 'ACTIVA'` — CIT Completo listings (`PUBLICADO_PENDIENTE_CERTIFICACION`/`PUBLICADO_CERTIFICADO`) never appeared in the grid at all, regardless of how many were published. (2) **purchase entry point** — even reaching a CIT Completo listing by direct link, `publicacion-detalle.tsx`'s only buy button computed `activa = pub.estado === 'ACTIVA'` and hid the button entirely otherwise; the button that did exist called the old single-payment `/comprar`, whose backend (`iniciarCompra()`) explicitly rejects any `estado !== 'ACTIVA'` with a 409 — CIT Completo purchases were structurally impossible through the UI. (3) **post-purchase tracking** — the originally-documented gap, confirmed unchanged: zero "mis compras" screen, zero frontend callers for `/reservar`/`/confirmar-pago`.
  - **Fixed 2026-07-11 (layer 2 only, by priority — this unblocks CIT Completo sales from happening at all), commit `b27994f`, confirmed live via `getDeploy` (`6a5190ed499433000844ff77`, `state: ready`, `error_message: null`):** `publicacion-detalle.tsx` now branches on `pub.estado` and (new) `miReserva` — a per-viewer field added to `GET /api/v1/marketplace/[id]` (now optionally authenticated; `obtenerMiReservaActiva()` in `escrow.service.ts` looks up the viewer's own non-terminal reservation for this publicación, never exposed to other viewers). Shows "Reservar" (→ `/reservar`) for a fresh CIT Completo listing, a waiting message once reserved (`RESERVA_PENDIENTE`/`RESERVADA`), and "Pagar saldo" (→ `/confirmar-pago`) once the Taller seals the checklist (`SALDO_PENDIENTE`). `app/checkout/resultado/page.tsx` (the MercadoPago return page, previously 100% generic) now also recognizes `RESERVADA` (seña confirmada) and `RESERVA_VENCIDA` distinctly from generic `CANCELADA`; `GET /api/v1/escrow/pago/[txId]/estado` now also returns `plan` to support this.
  - **Fixed 2026-07-11 (layer 1, Prioridad 2 — CIT Completo listings now actually appear in the marketplace grid):** `app/api/v1/marketplace/route.ts`'s `estado` filter went from a single value defaulting to `'ACTIVA'` to an `estados: string[]` defaulting to `['ACTIVA', 'PUBLICADO_PENDIENTE_CERTIFICACION', 'PUBLICADO_CERTIFICADO']` (`PUBLICADO_PENDIENTE_CERTIFICACION` confirmed in-scope by Federico 2026-07-11) — deliberately excludes `RESERVADO`/`EJECUTANDO_LOGISTICA` (already claimed). All 5 state-filtered queries (main list + 4 facet-count queries) switched from `mp.estado = $1` to `mp.estado = ANY($1::text[])` for consistency between what's listed and what the facet counts show; the unrelated `totalActivas` stat query was deliberately left untouched — confirmed unused anywhere in `marketplace.tsx`, so changing it added risk for zero visible benefit. `components/rodaid/listing-card.tsx` (the actual card shown in the live grid, confirmed already correctly wired to `/marketplace/[id]` → `app/marketplace/[id]/page.tsx` → `publicacion-detalle.tsx`, no bug there) now shows "CIT Completo" instead of the generic "CIT" badge, and "Reservar" instead of "Comprar protegido", for any non-`ACTIVA` listing — matching what the detail page (layer 2, fixed earlier) actually shows once clicked.
  - **Fixed 2026-07-11 (layer 3, Prioridad 3 — the "mis compras" screen itself, closing item 4 end to end):** new `GET /api/marketplace/mis-compras` + `obtenerMisCompras()` in `garaje.service.ts` (mirrors `obtenerMisPublicaciones()`'s query shape, but from `escrow_transacciones WHERE comprador_id = $1`, joined to `marketplace_publicaciones` + `bicicletas`), a new `useMisCompras()` SWR hook in `lib/garaje-digital.ts`, a new `components/rodaid/mis-compras.tsx` (same list-item shell as `mis-publicaciones.tsx`, its own `ESTADO_COMPRA` label map covering both the generic flow and the three CIT Completo sub-states), rendered alongside `<MisPublicaciones />` in `app/garaje/page.tsx`. New `components/rodaid/cuenta-regresiva.tsx`: a genuinely-ticking countdown (`setInterval` every 30s) against `reservaVenceEn`, unlike `PipelineEstado` in `garaje-digital.tsx:431-475`, which only recomputes on parent re-render. Deliberately NOT part of this build: the "3 business day repurchase priority" from the disputes design doc (item above) — the mechanic itself was defined later the same day (see punto 5 in the disputes doc, above: visible-but-blocked-to-third-parties, same `iniciarReservaCitCompleto()` saldo-completo path, no new payment flow needed), but the actual implementation (the logic distinguishing the priority buyer from a new one) is still not built.

  **Item 4 fully closed 2026-07-11, all three layers.** Summary of the whole day's work on this item: what started as "add a missing mis-compras screen" turned out, on re-audit, to be a three-layer gap — CIT Completo listings weren't discoverable in the marketplace grid, couldn't be purchased even by direct link, and had no post-purchase tracking. All three are now fixed and confirmed live: Prioridad 1 (purchase flow, commit `b27994f`, deploy `6a5190ed499433000844ff77`), Prioridad 2 (grid visibility, commit `a4c98e9`, deploy `6a519a717dde0400089e6fd9`), Prioridad 3 (this commit, mis-compras screen). Related, found the same day and deliberately left alone: item 13 below (the orphaned `/marketplace` prototype route).

- **Item 13 — `app/marketplace/page.tsx` (a separate route from the real, live marketplace grid) calls a backend endpoint that queries a table that doesn't exist.** Found 2026-07-11 while investigating item 4. `app/marketplace/page.tsx` calls `GET /api/v1/buscar`, whose SQL queries `FROM publicaciones p ... JOIN cits c ...` — a full-repo grep of every migration confirms no table named bare `publicaciones` was ever created (only `marketplace_publicaciones` exists); this query would fail at runtime with "relation does not exist". This route is not linked from `Nav`, the footer, or any live component — the real marketplace grid users actually see is `components/rodaid/marketplace.tsx` (rendered on the homepage, `app/page.tsx`), which correctly calls `GET /api/v1/marketplace` against `marketplace_publicaciones`. `/marketplace` and `/api/v1/buscar` appear to be an orphaned early prototype, same category as `stravaWebhookService.ts` (found earlier the same day). Zero real-user impact confirmed — deliberately left untouched this pass, documented here only as a finding.

- **Item 14 — three separate, easily-confused chat systems exist; only one had a real static knowledge base, and it didn't know about CIT Express/Completo or the disputes scheme.** Investigated 2026-07-11. (1) `components/rodaid/SoporteChat.tsx` (floating widget, every page via `app/layout.tsx`): **no AI at all** — `detectarRespuesta()` is a keyword `if/else` against a fixed `RESPUESTAS_AUTO` dict. Its "notify admin" call (`POST /api/v1/soporte/mensaje`) hits an endpoint that **doesn't exist anywhere in the repo** (`app/api/v1/soporte/` was never created) — fails silently every time via `.catch(() => undefined)`, nobody would ever notice. **Fixed same pass:** its `hurto` canned reply had the same stale `$4.500 ARS` figure already fixed today in `denuncia-comunitaria.tsx` — replaced with the same non-hardcoded phrasing ("gratis si tu bici ya tiene un CIT activo; si no, te mostramos el costo exacto antes de confirmar"). The dead `/api/v1/soporte/mensaje` endpoint itself was NOT built — out of scope, just noted. (2) RODAID-GPT (`app/asistente/page.tsx`, dedicated page, `POST /api/gpt/consulta` — not `/api/v1/gpt/consulta`): Claude Sonnet (`claude-sonnet-4-6`) via Netlify AI Gateway (`rodaid-gpt.service.ts`). Deliberately personal-only — its system prompt is 100% dynamic per-user context (own bikes, CIT status, BFA anchor, security heatmap of the user's zone), zero static business/pricing knowledge by design. (3) The Footer's public FAQ assistant (`FaqFooterLink` → `rodaid-faq.service.ts`): Claude Haiku 4.5 via Netlify AI Gateway, public/no-session, holds a genuine static knowledge base (`MISION`, `VISION`, a 14-entry `HITOS` array, `MARCO_LEGAL`, `ENLACES`) — but its `HITOS` entry for "CIT" described one generic product, no Express/Completo split, no pricing, no disputes scheme, and the system prompt's own Regla de Oro #1 explicitly forbids inventing prices not in the knowledge base (confirmed it would correctly refuse rather than hallucinate a number — good existing design, just missing content).
  **Fixed same pass, item 14's actual deliverable:** two new `HITOS` entries in `rodaid-faq.service.ts` — "CIT Express y CIT Completo" (conceptual description of both products and the reserve→verificación→saldo sequence, **no hardcoded prices**, explicitly defers to `rodaid.net/precios` for the current exact value — deliberate, given the auto-indexing mechanism built earlier today means a hardcoded number here would drift the same way the ones fixed all day already had) and "Consecuencias de actuar de mala fe" (firm but deliberately vague per Federico's instruction — no mention of the amarillo/naranja escalation thresholds, the cancellation count that triggers human review, or how evidence is defined; only that detection/sanction mechanisms exist, reputation is tracked, the payment system is designed so nobody works for free off someone else's scam, severe cases can cost the account, and everything is anchored on-chain with verifiable identity). Added `precios: '/precios'` to `ENLACES` (didn't exist before) and referenced it in the system prompt's "INTERFAZ DE RESPUESTA" line so the assistant actually offers the link.

- **Item 5 (FIXED 2026-07-11) — `garaje-digital.tsx` had no "someone reported this bike as stolen, please confirm" banner.** Re-verified before building: the backend endpoints (`GET /api/v1/bicicletas/[id]/denuncia-tercero`, `POST .../denuncia-tercero/[denunciaId]/confirmar`) and the `CANAL_POLICIAL_NO_DISPONIBLE` guard blocking Fase 7's caso 3 entry point were both exactly as documented — still zero real-world urgency, since no real `denuncias_terceros` row can reach `ESPERANDO_PROPIETARIO` in production while that guard holds. Built anyway (cheap, self-contained, ready for whenever the guard lifts): new `DenunciaTerceroBanner` in `garaje-digital.tsx` (same file, same idiom as the other `ActivoCard` sub-blocks like `PipelineEstado`/`AnclajeBfaBloque` — not a separate file), one fetch on mount (no polling — the urgent notification channel is push/email, not this banner), renders nothing unless `denuncia.estado === 'ESPERANDO_PROPIETARIO'`. Deliberately does NOT gate on `activo.estado` — a third party can denounce any matching numero de serie regardless of the bici's own CIT state. Confirm/deny buttons call the confirmar endpoint with `{ confirmaRobo: boolean }`.

- **Item 8 (FIXED 2026-07-10) — `app/sobre/page.tsx`'s "Que es el CIT" section described one flat product at the old $18.000 ARS price; there are actually two products (CIT Express $5.100, CIT Completo $28.500 — see `parametros_pricing_cit`) with genuinely different processes, not just different prices.** Originally spanned three separate spots in the same file: the main "Que es el CIT" card (a single narrative describing only CIT Completo's physical-inspection process, plus a "Valor del tramite" stat hardcoding `18.000`), the "Talleres aliados" benefits bullet (`'Nueva fuente de ingresos por CIT ($18.000 ARS)'`), and the "Alcances del marketplace" stat grid (`{ val: '$18.000', lbl: 'ARS por CIT emitido' }`). Fixed by rewriting the main card into two side-by-side blocks (CIT Express / CIT Completo, each with its own price, vigencia, and audience), using the two real photos of the redesigned decals Federico provided (`public/cit-express.png`, `public/cit-completo.png`) — and by pointing both of the other two stray `$18.000` occurrences specifically at CIT Completo's real price ($28.500), per Federico's confirmation that CIT Completo is the only one of the two products that actually pays the taller aliado and the only one that participates in the marketplace (CIT Express is self-service, circulation-only). Still open, deliberately not verified/touched in this pass: `components/rodaid/solicitar-verificacion-modal.tsx` (lines 98, 120) also hardcodes `$18.000 ARS` for CIT *renewal* specifically — may legitimately be a different number for a different flow (renewal vs. initial purchase), but should be verified against current pricing rather than assumed correct.

- **Item 9 (FIXED 2026-07-10) — the "60/40 aliado/RODAID split" claim in `app/sobre/page.tsx` described a discontinued commission model.** Found while designing item 8's fix (2026-07-10): `app/sobre/page.tsx` stated a flat 60/40 split in two places — the Vendedores benefits bullet (`'60% de la tarifa CIT va al taller aliado'`, actually in the **Vendedores** column, not Talleres aliados as first assumed) and the Alcances del marketplace stat grid (`{ val: '60 / 40', lbl: 'Split aliado / RODAID' }`). Confirmed against `rodaid_pricing_model.xlsx` (hoja "Margen Neto Marketplace"): the 60/40 is explicitly labeled in that sheet as the "MODELO ANTERIOR" for the taller's sale commission, superseded by "Fee de Exito Proporcional Compartido (RODAID + Taller)" — which is exactly what `pricing-cit.service.ts` and `20260708000001_create_parametros_pricing_cit.sql` already implement: the Taller Aliado keeps 100% of `cit_completo_fee_verificacion_ars` ($18.000, verified) and 100% of `cit_completo_fee_logistica_ars` ($20.000, verified) — neither is shared with RODAID — and only the Fee de Exito (`cit_completo_fee_exito_pct` = 0.02, 2% of the sale value, charged on a completed sale) splits 50/50 (`cit_completo_fee_exito_split_rodaid_pct` = 0.50, verified) between RODAID and the Taller. The 60/40 wasn't a rounding of the 50/50 — it described a commission model that no longer exists. Fixed by replacing the Vendedores bullet with `'El taller aliado cobra verificacion y logistica, y comparte el exito de cada venta'` (matches the short-bullet tone of its 3 siblings in that column, instead of the longer sentence first proposed) and the stat grid entry with `{ val: '50/50', lbl: 'Split del Fee de Exito' }`.

- **Item 10 (FIXED 2026-07-10, logística figure and fee name corrected 2026-07-11) — `app/taller/page.tsx` (Panel del Taller Aliado) repeated the same discontinued $18.000/60% figures items 8 and 9 already fixed elsewhere.** Found 2026-07-10, while auditing the rest of the app for the same stale pattern. Two occurrences: line 27, `const ingresos = total * 10800` (fed the "Ingresos ARS" stat card, hardcoding a single CIT at the old $18.000 price times the old 60% split); and the static "Info retribución" block (`'Retribución por CIT: $10.800 ARS'` / `'Recibís el 60% de cada CIT emitido.'`). Confirmed via grep this didn't repeat anywhere else in the Taller panel (including `components/rodaid/inspecciones.tsx`). Fixed by switching to the real fee structure instead of a flat percentage: `ingresos = total * 33000` (verificación $18.000 + $15.000, both 100% taller per `pricing-cit.service.ts` — the two fixed, guaranteed components of a completed CIT Completo), the stat label renamed to `"Ingresos garantizados (ARS)"` to signal this is a floor, and the retribución block rewritten to `"Retribución por CIT: $33.000 ARS garantizados"` / `"$18.000 por la verificación + $15.000 por el embalaje — el 100% es tuyo. Si la bici se vende, sumás además el 50% del fee de éxito (2% del valor de venta)."`. Deliberately does NOT fold the variable éxito fee into `stats.ingresos` — it depends on each bici's sale value, which `/api/v1/inspector/cit` (the endpoint this stat is computed from) doesn't return; hardcoding a guess there would just create a new orphaned number instead of fixing one. Shipped 2026-07-10 with a $20.000 "logística" figure (then-current); corrected the next day to $15.000 once Federico confirmed that value for `parametros_pricing_cit`, and the label itself renamed from "logística" to **"embalaje"** — Federico clarified the taller's fee only covers packing the bike for dispatch; actual transport/shipping is a separate service to be executed by an external logistics company (Correo Argentino, Andreani, or similar, still undefined) with its own price, not yet part of this fee. **Confirm the `parametros_pricing_cit` migration for $15.000 actually landed before treating either the amount or the "embalaje" framing as settled** (see item 11 for the same caveat) — this session never saw or ran that migration, only Federico's verbal confirmation.

- **Item 11 (FIXED 2026-07-11) — `app/academia/usar-rodaid/page.tsx` ("Cómo usar RODAID al máximo") described CIT as one undifferentiated 6-step process, with the same stale $18.000/60-40 figures, plus two more unverified numbers found during the fix.** Módulo 1 mixed CIT Express territory (Paso 1-2: registro, agregar bici) with CIT Completo territory (Paso 3-6: taller inspection, BFA anchor, public verify) as a single flow, same problem "Qué es el CIT" had before item 8. Paso 4 read `'El taller emite el CIT ($18.000 ARS — 60% va al taller, 40% a RODAID)'` — same discontinued price/split as items 9/10. Fixed by rewriting Módulo 1 to clearly separate CIT Express ($5.100, self-service) from CIT Completo/Transferencia, explaining the real sequence Federico confirmed: publicás gratis en el Marketplace (con CIT Express activo) → el comprador reserva y paga la seña → esa seña financia la verificación de 20 puntos del Taller Aliado → una vez sellada, se completa la venta (logística + saldo) — and using the real fee structure (verificación $18.000 + **embalaje** $15.000, both 100% taller; éxito 2% del valor de venta, split 50/50 RODAID/Taller). The "embalaje" naming (not "logística") reflects the same item 10 clarification: the taller's fee only covers packing, not the actual transport, which a separate external logistics company will execute at a price not yet defined. The two additional unverified figures found while auditing item 11 turned out to both be fixable: the `'80% para vos, 20% RODAID'` marketplace split in Módulo 2 was replaced with a description with no specific percentage (`'RODAID cobra una comisión de éxito, compartida con el Taller Aliado... Vos recibís el resto del valor de venta.'`), since no matching flat-percentage constant exists in `escrow.service.ts` for the generic plan that bullet seemed to describe. The km-threshold claims in Módulo 4 (500/800/1000/1500 km) turned out to reference **dead code**: `src/services/stravaWebhookService.ts` has exactly that `UMBRAL_ALERTA_KM = 500` logic, but a full-repo grep confirmed nothing imports that file — it's an orphan, never wired to any route. The real, live Bici-Salud system is `src/services/iot-mantenimiento.service.ts` (Hito 17, wired to `app/api/v1/iot/mantenimiento/analizar/route.ts`), which analyzes accelerometer telemetry via Claude and returns a probability + severity per component (`cadena`/`cubiertas`/`servicio`) — no km milestones at all. Módulo 4 was rewritten to describe this real system instead. **The $15.000 figure and the "embalaje" framing (here and retroactively in item 10) were confirmed verbally by Federico but this session never saw or ran a migration changing `cit_completo_fee_logistica_ars`/renaming it in the database — verify the real column/parameter naming and value before relying on this elsewhere.**

- **Item 12 — the "logística" wording renamed to "embalaje" in items 10/11 was scoped ONLY to text describing the taller's fee; two other user-visible "logística" occurrences were deliberately left untouched because they mean something different.** `components/rodaid/admin-dashboard.tsx`'s Identidades tab (`"Habilitá o revocá las API Keys de aseguradoras o empresas de logística (Hito 16)."`) and the matching comment in `app/api/v1/admin/panel/identidades/api-keys/route.ts` both refer to external logistics/insurance companies as third-party API consumers — unrelated to the CIT fee. `components/rodaid/mis-publicaciones.tsx`'s `EJECUTANDO_LOGISTICA` pipeline state label (`"En logística — esperando el saldo"`) names the general post-verification coordination phase, not the fee specifically — and per item 10's clarification, that phase now genuinely spans both the taller's embalaje and the separate external transport, so "logística" may still be the more accurate umbrella term there. Left as-is; revisit only if the state label itself is ever reported as confusing.

### Fase 6 (CIT Completo): reserve/pay endpoints + a money-integrity audit that found and fixed 4 real bugs (2026-07-09)

`POST /api/v1/marketplace/[id]/reservar` and `POST /api/v1/marketplace/[id]/confirmar-pago` (delegating to `iniciarReservaCitCompleto()` / `confirmarPagoCitCompleto()` in `escrow.service.ts`) close the buyer-facing side of the CIT Completo escrow flow. Two paths:
- Bici not yet certified (`PUBLICADO_PENDIENTE_CERTIFICACION`): `/reservar` charges a **sena** (funds the Taller's 20-point verification), publicación → `RESERVADO`, escrow → `RESERVA_PENDIENTE` → (webhook) `RESERVADA`. Once `aprobarInspeccionFisica()` seals the checklist (publicación → `EJECUTANDO_LOGISTICA`), `/confirmar-pago` charges the **saldo** (precio + logística), escrow → `SALDO_PENDIENTE` → (webhook) `FONDOS_RETENIDOS`.
- Bici already certified (`PUBLICADO_CERTIFICADO`, from a prior expired reservation): `/reservar` skips the sena entirely — no second verification, the Taller already got paid once — and charges the full saldo in one payment, escrow → `SALDO_PENDIENTE` directly, publicación → `EJECUTANDO_LOGISTICA` directly. `/confirmar-pago` never applies to this path (no `RESERVADA` row ever exists for it).

`webhookPago()` now branches on a `TRANSICIONES_APROBADO` map keyed by the escrow row's **current `estado`** (never on `mp_pagos.concepto`, which is purely a bookkeeping label): `DEPOSITO_PENDIENTE→FONDOS_RETENIDOS` (generic flow, unchanged), `RESERVA_PENDIENTE→RESERVADA` (fires the `escrow.verificacion_solicitada` notification to the Taller), `SALDO_PENDIENTE→FONDOS_RETENIDOS` (both origins above land here identically).

**A dedicated money-integrity audit of this whole area (same date, prompted explicitly: "no otro bug puntual, una auditoría completa") found and fixed four real bugs before this shipped** — the kind of "automatic system action decided against stale information" class of bug, checked systematically across every expiry clock, every scheduled worker, and every notification path:
1. **`reserva_vence_en` was never cleared once a transaction reached `FONDOS_RETENIDOS`.** Any CIT Completo sale taking longer than 48h end-to-end (a normal timeline, not an edge case) would get caught by the next `procesarReservasVencidas()` sweep and **downgraded from `FONDOS_RETENIDOS` back to `CANCELADA`** — a fully-paid transaction silently un-paid, publicación reopened for a different buyer. Fixed by clearing `reserva_vence_en = NULL` in the same `UPDATE` that sets `FONDOS_RETENIDOS` in `webhookPago()`'s `SALDO_PENDIENTE` branch.
2. **No reconciliation against MercadoPago before reverting an expired reservation.** A lost or late-arriving webhook meant `procesarReservasVencidas()` would cancel a reservation that MercadoPago had already actually approved, with no trace left anywhere. Fixed: the sweep now calls a new `buscarPagosPorExternalReference()` (searches MP's `/v1/payments/search` by `external_reference`, i.e. the escrow transaction id) for any row with a still-`PENDIENTE` `mp_pagos` entry, excludes payment_ids already known locally, and — if MP reports a genuinely new approval — delegates to the normal `webhookPago()` instead of reverting. If the MP lookup itself errors, the row is **not** reverted that pass (retried next run) — erring toward not cancelling over erring toward cancelling a possibly-paid reservation.
3. **A pago-aprobado-arrives-for-an-already-terminal-row event logged nothing.** Even after fix #2, a narrow (seconds-wide) race remains where MercadoPago approves a payment in the exact window between the reconciliation check and the revert's commit. `webhookPago()` now `console.error`s explicitly (with transaccionId/paymentId/monto) whenever an `approved` payment lands on a `CANCELADA`/`RESERVA_VENCIDA` row, so this can never fail 100% silently even though it can't be fully eliminated without holding a DB lock across an HTTP call (impractical).
4. **`procesarReservasVencidas()`'s SQL only filtered on the `reserva_vence_en` timestamp, not on `estado`** — unlike `procesarAutoReleases()`, whose `SELECT` filters `WHERE estado = 'EN_CAMINO'` directly, which is exactly why that function turned out NOT to share bug #1's failure mode (confirmed by tracing `abrirDisputa()` moving `EN_CAMINO→DISPUTADA` without clearing `auto_release_en` — harmless there only because the outer query already excludes non-`EN_CAMINO` rows). Added the equivalent `AND estado IN ('RESERVA_PENDIENTE', 'RESERVADA', 'SALDO_PENDIENTE')` to `procesarReservasVencidas()`'s query as defense-in-depth, so a future code path that forgets to clear the timestamp can't repeat bug #1 either.

Confirmed live in production via `getDeploy` (`6a500438bb7c68000803b3cd`, `state: ready`, `error_message: null`, `database_migrations.files: []` — this piece needed no schema changes, every column/enum it uses already existed from earlier Fase 3/6 migrations).

**Known gap, still open, documented with an explicit TODO in both `webhookPago()` and the state-machine doc comment at the top of `escrow.service.ts`:** there is no "confirm sale/delivery" closing endpoint for CIT Completo yet. A transaction that reaches `FONDOS_RETENIDOS` via `SALDO_PENDIENTE` sits there **forever** — nobody gets paid (not the vendedor, not the Taller for logística/éxito). The generic `confirmarEnvio`/`confirmarEntrega` endpoints are not a substitute: they model postal shipping by the vendedor, not the Taller-coordinated logística CIT Completo actually needs, and they would only ever pay out the vendedor (no `registrarLiquidacionAliadoFeeLogistica`/`...FeeExito` exist yet, unlike `registrarLiquidacionAliadoFeeVerificacion`, which already fires correctly at `aprobarInspeccionFisica()` seal time). Building this closing endpoint is real, scoped-out future work — not a minor detail.

### Esquema de disputas CIT Transferencia — diseño acordado 2026-07-10, actualizado 2026-07-11 (Esquema 3 y 4 nuevos) y 2026-07-12 (resoluciones de Esquemas 3/4 + mecanismo de canon), pendiente de implementar

**Nada de lo siguiente está implementado todavía**, salvo lo marcado explícitamente como ya implementado. Es la especificación completa que Federico definió el 2026-07-10 para el flujo de venta/disputa del CIT Completo ("CIT Transferencia"), ampliada el 2026-07-11 con dos esquemas nuevos (reclamo de titularidad fuera de plataforma, e impugnación de denuncia falsa) y el 2026-07-12 con la resolución de varios puntos pendientes de esos dos esquemas más un mecanismo de canon nuevo para iniciar disputas — guardada como referencia para una sesión dedicada aparte. Quedan puntos sin resolver del todo (listados al final de cada esquema), así que no es apta para implementar de punta a punta sin antes cerrar esos puntos con Federico.

**Precios vigentes (ya en producción, no son parte de lo pendiente):** CIT Express $5.100 ARS — identidad básica, circulación, 12 meses. CIT Completo/Transferencia $28.500 ARS — habilita venta en el Marketplace, vigente hasta la transferencia de dominio.

**Estructura de fees del CIT Transferencia (ya implementada):** Fee de Verificación $18.000 — 100% Taller Aliado. Fee de Logística $15.000 (ajustado 2026-07-10, antes $20.000 — equivalente a USD 1) — 100% Taller Aliado. Fee de Éxito 2% del valor de venta — 50% RODAID / 50% Taller Aliado, solo si la venta se concreta. **Total garantizado al Taller por CIT Completo sellado: $33.000** (antes $38.000).

#### Esquema 1 — Comprador ↔ Vendedor (particulares, usuarios)

**El flujo normal:** (1) el vendedor tiene un CIT Express activo (12 meses) → puede publicar gratis en el Marketplace; (2) un comprador reserva la bici y paga la seña; (3) la seña financia la verificación de 20 puntos del Taller Aliado (CIT Transferencia); (4) el taller sella la verificación → el CIT queda inmutable (no se cancela nunca, es un hecho técnico verificado); (5) el comprador confirma y paga el saldo → venta concretada.

**Si la venta NO se concreta:**

- **Caso A — comprador sin fondos / no confirma a tiempo (ya implementado, Fase 5):** pasadas 48hs desde el sellado del taller sin confirmación del comprador, la seña NO se devuelve (financió el trabajo real del taller, que ya se ejecutó); la publicación vuelve a estar disponible — como el CIT ya está sellado, el próximo comprador paga directo el saldo, sin nueva seña ni nueva verificación. **Prioridad de recompra por 3 días hábiles — RESUELTO 2026-07-11:** la publicación sigue visible en el marketplace durante los 3 días (no se oculta), pero el sistema bloquea cualquier intento de reserva de un tercero hasta que pasen los 3 días hábiles. El comprador prioritario tiene que pagar de nuevo dentro de los 3 días para confirmar — no alcanza con solo manifestar intención (sin botón de "reservar sin pagar"). Qué paga: como la bici ya tiene el CIT Completo sellado (ya se pagó en la venta original que se cayó), el comprador prioritario paga directo el valor de la bici más el embalaje/logística si todavía no se ejecutó — mismo trato que un comprador nuevo de una publicación `PUBLICADO_CERTIFICADO`, mismo camino técnico que ya calcula y cobra `iniciarReservaCitCompleto()` en su rama `PUBLICADO_CERTIFICADO` (endpoint `/reservar`, no `/confirmar-pago`) — **no hace falta un flujo de pago nuevo**. No vuelve a pagar el CIT de Transferencia (verificación ya cubierta). Implementación técnica pendiente: cómo se identifica "es el mismo comprador" en el endpoint de reserva/confirmación (¿por `comprador_id` de la transacción vencida más reciente sobre esa publicación?), y qué pasa si pasan los 3 días hábiles sin que confirme (la publicación se abre a cualquiera, sin más trámite) — `iniciarReservaCitCompleto()` hoy no distingue comprador prioritario de uno nuevo.

- **Caso B — disputa entre comprador y vendedor (nuevo, sin implementar):** el CIT nunca se cancela — es un hecho técnico, separado del resultado comercial de una venta puntual. La venta puntual sí se cancela — la publicación vuelve a `PUBLICADO_CERTIFICADO`, disponible sin nueva verificación. **Confirmado por investigación de Claude Code (2026-07-10): el mecanismo de disputa genérico existente (`abrirDisputa()`/`resolverDisputa()` en `escrow.service.ts`) NO sirve para esto tal cual** — es alcanzable por accidente desde CIT Completo (ambos flujos llegan a `FONDOS_RETENIDOS`), pero `resolverDisputa()` reactiva la publicación con `UPDATE marketplace_publicaciones SET estado = 'ACTIVA' WHERE estado = 'PAUSADA'`, nombres de estado exclusivos del flujo viejo — para una publicación CIT Completo ese UPDATE no matchea ninguna fila y no hace nada (sin error, pero sin efecto). Ninguna de las dos funciones referencia `tx.plan` en ningún lado (y de hecho `tx.plan` no sirve para distinguir CIT Completo del flujo genérico — ver la nota sobre `escrow_transacciones.plan` en la sección de transferencia de titularidad más arriba; el campo confiable es `aliado_id IS NOT NULL`). Hay que construir un mecanismo de disputa nuevo, específico para CIT Completo, no reusar el existente. Reputación del vendedor en escalones: 1ra cancelación con evidencia → AMARILLO automático, solo advertencia, sin revisión humana. 2da cancelación con evidencia → SIEMPRE dispara revisión humana (nunca automática) antes de cualquier sanción — un humano mira la evidencia de ambos casos y decide si se confirma o se descarta. Naranja confirmado por un humano → cuenta marcada como alto riesgo/posible estafador, más una deuda hacia RODAID por el costo de verificación ya ejecutado (no queda "gratis" para él) — mecanismo de cobro (descuento de próxima venta vs. bloqueo hasta saldar) a definir. Extinguir la cuenta → NUNCA automático, siempre decisión humana posterior sobre el patrón completo (no necesariamente ligada al segundo caso).

**Protección contra fraude a escala (bicicleterías/revendedores):** múltiples CIT de Transferencia sin venta concretada, por sí solo, NO es prueba de mala fe (puede ser alto volumen legítimo o mala suerte genuina) — pero SÍ prioriza la cola de revisión humana para las futuras cancelaciones con evidencia de ese vendedor. Tope objetivo a definir: cantidad de "verificaciones financiadas por seña sin venta concretada" que un mismo vendedor puede acumular en una ventana de tiempo (ej. por mes) antes de que sus publicaciones nuevas dejen de financiarse con la seña del comprador — el vendedor pasaría a pagar la verificación por adelantado hasta regularizar su historial. Objetivo: quitar el incentivo económico del fraude a escala sin necesitar demostrar colusión caso por caso. Señales adicionales de patrón de fraude coordinado (para detección, no para sanción automática): mismo comprador/grupo chico de cuentas reservando múltiples bicis de un mismo vendedor; cuentas de comprador muy nuevas sin historial; ratio de cancelaciones muy por encima de vendedores comparables.

#### Esquema 2 — Taller Aliado ↔ Comprador (vía el CIT Transferencia)

El Taller Aliado siempre cobra el 100% del Fee de Verificación ($18.000) y el 100% del Fee de Logística ($15.000) por el trabajo que efectivamente realiza — sea cual sea el resultado final de la venta (Caso A o B de arriba). Recibe además el 50% del Fee de Éxito (2% del valor de venta) SOLO si la venta se concreta. El taller nunca pierde el pago por la verificación ya ejecutada, incluso si la venta se cae — protege al taller de trabajar gratis por una disputa ajena a su trabajo técnico.

**Sanción a Talleres Aliados que intenten defraudar a la plataforma (nuevo, sin implementar):** aplica solo cuando hay evidencia presentada (mismo principio que el Esquema 1 — sin evidencia, no hay sanción). Con evidencia confirmada: se descuenta el monto correspondiente al CIT de Transferencia en cuestión, y queda registrado el antecedente en el sistema por 24 meses. Mecanismo exacto de "descuento" (de qué próxima liquidación, cómo se aplica) — pendiente de definir.

#### Esquema 3 — Reclamo de titularidad (venta fuera de la plataforma, sin `/transferir`)

Caso de negocio (confirmado 2026-07-11): un comprador adquiere una bici ya registrada en RODAID por otro dueño, pero la venta se hizo por fuera de la plataforma (sin usar el Marketplace ni `/transferir`) — el dueño anterior nunca confirmó nada, la base de datos sigue diciendo que la bici es suya, mientras el comprador la tiene físicamente en su poder. Por qué esto no puede resolverse solo con la palabra del comprador: permitir que alguien cambie `propietario_id` sin ningún tipo de confirmación crearía un vector de fraude — cualquiera podría "reclamar" una bici ajena mientras el dueño real todavía la tiene, sin enterarse.

**Diseño confirmado — mismo patrón que el sistema de disputas del Marketplace (evidencia + revisión humana):** el comprador inicia un "reclamo de titularidad" para una bici ya registrada a nombre de otra persona, sube evidencia de la compra (comprobante de pago, capturas de chat/WhatsApp con el vendedor, mail — mismo mecanismo de upload que el sistema de disputas del Marketplace), y un humano de RODAID revisa el caso antes de autorizar la transferencia forzada. Solo con esa autorización humana se ejecuta `transferirTitularidadBicicleta()` con un `motivo` nuevo (ej. `'reclamo_con_evidencia'` — **hoy el CHECK constraint de `cit_transferencias.motivo` solo acepta `'venta_marketplace'`/`'transferencia_manual'`; hace falta extenderlo cuando se construya esto**), preservando el CIT igual que los otros dos caminos.

**Ajuste crítico (confirmado 2026-07-11) — peor caso: estafa/robo que intenta "blanquearse" vía el propio mecanismo de reclamo.** Escenario de riesgo: alguien roba (o compra a sabiendas de que es robada) una bici registrada a nombre de un tercero ajeno por completo al reclamo, y usa evidencia falsa/fabricada para que un revisor humano apruebe la transferencia — sin que la víctima real se entere de que está pasando. A diferencia de las disputas del Marketplace (donde ambas partes ya interactuaron dentro de RODAID, dejando rastro), acá el dueño anterior nunca participó — el reclamo no puede resolverse escuchando una sola campana. Por eso el flujo de resolución tiene que incluir al dueño actual registrado:
1. Al iniciarse el reclamo, se notifica AUTOMÁTICAMENTE al dueño actual registrado — aviso claro de que alguien está reclamando la titularidad de su bici, con un plazo para responder.
2. Si el dueño actual responde y NIEGA la venta → el reclamo se rechaza automáticamente (sin revisión humana adicional) — es evidencia de mala fe clara del reclamante, y debe quedar como antecedente grave en su cuenta (mismo criterio de reputación del Esquema 1).
3. Si el dueño actual no responde dentro del plazo → recién ahí pasa a revisión humana con la evidencia presentada — el revisor debe ver explícitamente que hubo silencio, no confirmación, como dato relevante para su decisión.
4. Si el dueño actual CONFIRMA que sí vendió → se aprueba directo, sin revisión humana adicional (el interesado real ya lo confirmó).

**Pendiente de definir en detalle (para cuando se construya):** (1) ¿se notifica al dueño anterior antes de resolver el reclamo? — RESUELTO, ver "ajuste crítico" arriba: sí, siempre, es obligatorio. (2) ¿Cuánto tiempo se le da al dueño anterior para responder antes de escalar a revisión humana? — **RESUELTO 2026-07-12: 48 horas**, mismo plazo que ya usa el sistema en otras partes. Antes de escalar a revisión humana, el sistema promueve primero un cruce de datos automático con el Ministerio Público Fiscal — el mismo mecanismo que `ejecutarCrossReference()`/`clasificarNivelCIT` ya usa para CIT Express — es decir, el reclamo de titularidad se trata, en ese primer paso, como si el reclamante estuviera solicitando un CIT Express nuevo sobre esa bici. Solo si ese cruce no resuelve nada, pasa a revisión humana. (3) ¿Quién tiene el rol de "revisor"? — **RESUELTO 2026-07-12:** un humano de RODAID (staff/admin existente; no se define un rol dedicado nuevo por ahora).

**Prioridad: baja por ahora** — Federico decidió no construir esto todavía (etapa MVP, volumen bajo). Queda documentado como diseño confirmado para retomar cuando el volumen de este tipo de caso lo justifique.

#### Esquema 4 — Impugnación de denuncia falsa (vendedor deshonesto denuncia DESPUÉS de vender por afuera)

Caso de negocio (confirmado 2026-07-11): un vendedor publica y vende su bici por fuera de RODAID (redes sociales, sin usar `/transferir` ni el Marketplace), cobra, entrega la bici — y DESPUÉS, dentro de las 72hs, registra una denuncia de robo/hurto sobre esa misma bici en RODAID. La denuncia bloquea la bici en toda la red — el comprador real, que pagó de buena fe, queda con una bici marcada como robada sin haber hecho nada malo.

Por qué es más grave que un caso de denuncia común: el sistema de denuncias fue diseñado asumiendo buena fe del denunciante (ver Fase 7: "el silencio favorece al denunciante" para el caso de terceros). Acá el denunciante ES el propio vendedor, y la denuncia no es un error de detección — es una herramienta de estafa deliberada, usando el mecanismo de protección de RODAID en contra de un tercero inocente. Conexión directa con el Esquema 3: mismo principio de fondo — nadie debe quedar a merced de una sola versión de los hechos sin poder defenderse. El Esquema 3 protege al dueño legítimo de un reclamo falso; este Esquema 4 protege al comprador real de una denuncia falsa.

**Diseño confirmado:** un comprador cuya bici quedó bloqueada por una denuncia debe tener un mecanismo para objetarla/impugnarla — subiendo su propia evidencia de la compra (comprobante de pago, chat con el vendedor, transferencia bancaria, etc.), mismo patrón de evidencia + revisión humana que el resto del sistema. La impugnación dispara revisión humana obligatoria (mismo criterio que el Esquema 3 — nunca resolver escuchando una sola campana) del caso completo: la evidencia del denunciante original (si la hay) vs. la evidencia del comprador que impugna. Si se confirma que la denuncia fue falsa/de mala fe: se levanta el bloqueo sobre la bici, la titularidad se transfiere al comprador real (mismo mecanismo de `transferirTitularidadBicicleta()`, motivo similar al reclamo del Esquema 3), y el vendedor denunciante queda con un antecedente grave — mismo nivel de sanción que la mala fe confirmada del Esquema 1, o más grave todavía (esto es una denuncia falsa a sabiendas, no solo una cancelación de venta).

**Pendiente de definir en detalle:** (1) ¿cualquier usuario puede iniciar una impugnación, o solo alguien que demuestre algún vínculo previo con la bici? — **RESUELTO 2026-07-12:** solo alguien que demuestre algún vínculo previo con la bici. Medios de prueba aceptados, en orden de fuerza: factura de compra, recibo de escribano, fotos que muestren la posesión real del rodado, otros medios de prueba fehaciente y, en última instancia, testimonio de un testigo — aclarado explícitamente como NO determinante por sí solo, el más débil de los medios de prueba. (2) ¿Plazo para impugnar una denuncia antes de que quede firme? — **RESUELTO 2026-07-12 (corregido el mismo día): 15 días hábiles** — revisado al alza respecto a la primera respuesta (1 día hábil), para dar tiempo real a reunir evidencia seria (factura, recibo de escribano, fotos, etc.). (3) ¿Mismo "revisor humano" que el resto del sistema, o algo específico dado que acá hay una denuncia judicial de por medio (las denuncias reales pasan por el MPF) — ¿RODAID puede levantar el bloqueo por su cuenta si la denuncia judicial en sí sigue activa en el sistema del MPF, o solo puede desvincular el bloqueo INTERNO de RODAID sin tocar el estado legal real? Sigue sin resolver.

**Prioridad: baja por ahora**, misma etapa MVP — queda documentado como diseño confirmado para retomar junto con el Esquema 3 (comparten la misma infraestructura de "evidencia + revisión humana + notificación a la otra parte").

#### Archivo de jurisprudencia — pieza transversal para los 4 esquemas (confirmado 2026-07-11)

Federico pidió que cada resolución de conflicto (Esquemas 1-4, cualquier caso que pase por revisión humana) quede documentada como un "archivo de jurisprudencia" — casos resueltos, con la metodología aplicada, consultable por los propios usuarios de la plataforma. Objetivo: consistencia entre revisores (no se resuelven casos parecidos de forma arbitraria), y transparencia real hacia la comunidad sobre cómo funciona el sistema de protección.

**Visibilidad confirmada:** solo para usuarios logueados, dentro del Garaje Digital — no público sin cuenta. Reduce el riesgo de que alguien sin ningún vínculo con la plataforma use el archivo para reconocimiento del sistema antes de intentar explotarlo.

**Cuidado crítico de diseño (mismo criterio aplicado toda la sesión con el chat de soporte y las páginas públicas): transparencia sin enseñar a esquivar el sistema.**

Cada caso publicado debe mostrar: qué TIPO de conflicto fue (ej. "cancelación repetida con evidencia de mala fe del vendedor", "reclamo de titularidad por venta fuera de la plataforma"); CÓMO se resolvió, en términos generales (ej. "se confirmó la sanción", "se rechazó el reclamo por falta de evidencia suficiente"); el PRINCIPIO general aplicado (ej. "RODAID prioriza proteger al comprador cuando hay evidencia clara y el vendedor no responde en el plazo").

Cada caso NUNCA debe mostrar: nombres reales ni ningún dato que permita identificar a las partes (anonimizado — algo como "Caso #047", sin usuario/bici/fecha exacta identificable); el detalle técnico EXACTO del mecanismo de detección que disparó la revisión (ej. no decir "se detectó al llegar a la 2da cancelación en 15 días" — ese nivel de precisión es un manual de cómo esquivarlo la próxima vez); el contenido literal de la evidencia presentada por cualquiera de las partes.

**Diseño técnico (a definir en detalle cuando se construya, junto con los Esquemas 1-4):** cada vez que un revisor humano resuelve un caso (en cualquiera de los 4 esquemas), además de la resolución interna, se genera un resumen anonimizado para el archivo de jurisprudencia — probablemente un campo de texto que el propio revisor completa al resolver (no generado automáticamente, para mantener control humano sobre qué tan anonimizado y qué tan general queda el relato). Página nueva dentro del Garaje Digital (ej. `/garaje/jurisprudencia` o una sección del Garaje existente) que lista los casos, filtrable por tipo de conflicto. Esto depende de que los Esquemas 1-4 ya tengan su mecanismo de revisión humana construido — no tiene sentido construir el archivo de jurisprudencia antes que los sistemas que generan los casos. Se construye junto con o después de esos esquemas, no antes.

**Prioridad:** misma etapa que los Esquemas 1-4 — todos low priority para el MVP actual, quedan documentados juntos para una sesión de diseño/construcción dedicada más adelante.

#### Mecanismo de evidencia — RESUELTO 2026-07-10

Upload de toda evidencia posible (capturas, chats, WhatsApp, mail), no un formulario de motivos predefinidos. Mismo mecanismo reusado en los Esquemas 3 y 4.

#### Mecanismo de canon para iniciar disputas (confirmado 2026-07-12) — inspirado en la Oficina de Conciliación Civil y Comercial (OCC) de Mendoza

Federico investigó el modelo real de conciliación prejudicial de Mendoza (Ley 9.388, OCC, en funcionamiento desde febrero 2025) como inspiración para un mecanismo de canon dentro del sistema de disputas del Esquema 1 — con el objetivo de evitar el desgaste del personal de RODAID en reclamos frívolos. Diferencia importante con el modelo real: la OCC cobra una multa (medio JUS) por INASISTENCIA a la audiencia, no por iniciar el trámite, y solo cobra tasas si hay acuerdo — RODAID adapta el concepto, no la mecánica exacta.

**Diseño confirmado:** (1) el canon se cobra a quien INICIA la disputa (no a quien no coopere, a diferencia del modelo puro de la OCC) — desincentiva reclamos frívolos desde el arranque. (2) Se devuelve completo si el reclamo resulta legítimo — mismo principio de "el reclamo de buena fe no cuesta nada real" que ya rige en el Esquema 1 (mala fe) y el Esquema 3 (reclamo de titularidad); solo se pierde si se confirma que el reclamo fue infundado/de mala fe. (3) Monto: **20% del valor de la venta del rodado en disputa** — proporcional, no fijo, y sin techo máximo (confirmado explícitamente 2026-07-12, tras evaluar y descartar un techo). Se cauciona al momento de iniciar el trámite de arbitraje, por quien lo inicia.

**Por qué sin techo:** RODAID está formalizando un mercado de bicicletas que hoy opera casi completamente informal/en negro en Argentina — sin un filtro proporcional fuerte, el mecanismo de disputas podría generar una demanda de revisión (y eventualmente de intervención de profesionales del derecho) que un equipo chico no puede sostener, especialmente en operaciones de alto valor. Ajuste 2026-07-12: se reemplazó la primera propuesta ($2.500 ARS fijo) tras investigar que el sistema legal argentino no tiene un porcentaje fijo uniforme para cauciones (el CPCCN art. 199 deja el monto a discreción del juez, caso por caso, según "verosimilitud del derecho") — el 20% se tomó como referencia de un criterio real y objetivo (el Código General del Proceso de Colombia lo fija así para medidas cautelares) aplicado sobre la propia escala de precios de RODAID, más coherente con la lógica de "formalizar un mercado informal" que un monto fijo simbólico.

**Pendiente de definir para cuando se implemente:** en qué punto exacto del flujo de disputa se cobra este canon (¿al presentar la evidencia inicial, antes de que un humano revise?), y cómo se integra con el mecanismo de pago ya existente (MercadoPago, mismo patrón que el resto del sistema).

#### IA predictiva en el sistema de disputas — deliberadamente pospuesto para después del 2026-07-11

Federico planteó si el agente de IA podría ser "predictivo" dentro del flujo de disputas. Antes de construir nada hay que definir con precisión sobre qué predeciría (¿riesgo de un vendedor antes de que pase algo? ¿lectura automática de evidencia? ¿patrón de fraude a escala?) y mantener el límite ya acordado: el naranja (sanción seria, posible extinción de cuenta) **siempre** requiere revisión humana, nunca una decisión automática de IA. Uso seguro ya contemplado: usar señales (ej. múltiples CIT sin venta) para *priorizar* la cola de revisión humana, nunca para decidir el resultado. Ir más allá — que la IA decida o sugiera fuertemente sobre casos individuales — es un riesgo real de perjudicar vendedores honestos y quedar indefendible ante un reclamo de trato injusto; merece su propia sesión de diseño dedicada.

#### Puntos pendientes de definir, sin resolver del todo (no urgente)

(1) Mecanismo de evidencia — RESUELTO, ver arriba. (2) Mecanismo de cobro de la deuda del vendedor en naranja (descuento automático de próxima venta vs. bloqueo manual). (3) Tope numérico exacto de "verificaciones sin venta" antes de exigir pago adelantado al vendedor. (4) Conexión del mecanismo de disputa genérico con CIT Completo — RESUELTO, ver Esquema 1 Caso B (no está conectado, hay que construir uno nuevo). (5) Mecánica de la prioridad de recompra de 3 días hábiles — RESUELTO, ver Esquema 1 Caso A (visible pero bloqueada a terceros; el comprador prioritario paga el mismo saldo completo vía `iniciarReservaCitCompleto()`, sin flujo de pago nuevo; falta implementar la lógica que distinga comprador prioritario de uno nuevo). (6) Mecanismo de cobro/descuento a Talleres con fraude confirmado. (7) Alcance seguro de la IA predictiva en disputas — ver arriba, pospuesto a su propia sesión. (8) **Esquema 3, plazo de respuesta del dueño anterior y rol de revisor — RESUELTO 2026-07-12**, ver Esquema 3 (48hs + cruce automático con el MPF antes de escalar a revisión humana; revisor = staff/admin existente, sin rol nuevo). (9) **Esquema 4, quién puede impugnar y plazo — RESUELTO 2026-07-12**, ver Esquema 4 (solo quien demuestre vínculo previo con la bici, con los medios de prueba jerarquizados; 15 días hábiles). (10) **Esquema 4, punto 3 — sigue sin resolver:** si RODAID puede levantar el bloqueo interno de forma independiente del estado de la denuncia judicial real en el MPF. (11) **Nuevo (mecanismo de canon, ver arriba):** en qué punto exacto del flujo se cobra el canon del 20%, y cómo se integra con MercadoPago.

### Known gap, not urgent, not a bug: `aliados.tipo` (`taller`/`tienda`/`otro`) has zero effect on Panel de Inspecciones permissions

Found 2026-07-12 while diagnosing why a `tipo='tienda'` test aliado profile linked to an admin account could load `/taller` fully but hit `ensureRoleSession`'s error in the Panel de Inspecciones — confirmed by exhaustive grep that `tipo` was **not** the cause (see the session-loss explanation below), but the investigation surfaced a real, separate design gap worth recording.

Confirmed by reading every layer of the inspection panel's authorization: the client gate (`lib/inspecciones.ts:72-74`, `ensureRoleSession(['inspector', 'aliado', 'admin'], 'inspector')`), the server gate on both endpoints it calls (`app/api/v1/inspecciones/contexto/route.ts:22,32` and `app/api/inspector/cit/route.ts`, both `requireRole('inspector', 'aliado', 'admin')`), and the business logic in `inspeccion.service.ts` (grepped every occurrence of `tipo` in the file — all refer to the *bicycle's* type, never `aliado.tipo`). **None of them ever read `aliados.tipo`.** Today, any approved aliado — `taller`, `tienda`, or `otro` — has identical permissions to seal a 20-point physical inspection and sign the acta; the only real gate is the admin's manual approval in `resolverAliado()`, which doesn't consider `tipo` either.

This isn't a bug (nothing is broken), but it's a real product gap if the intent is that only aliados with genuine mechanical capability (`taller`) should be able to certify a physical inspection — a `tienda` (e.g., an accessories/apparel shop) can be approved as an aliado today and immediately gets full inspection-signing rights. Fixing this would mean adding an explicit `tipo` check somewhere in the chain above (most naturally in `resolverAliadoDeUsuario()` or the route-level `requireRole` gate) — doesn't exist today. Deliberately not fixed in this pass — Federico hasn't decided whether this restriction is wanted.

### Admin View-As — a real, non-destructive way for admins to preview/test any aliado-scoped panel (2026-07-12)

Built after two rounds of manual DB patching (linking a test `aliados` row, then a real one — "Esquina Cicles" — to Federico's own `usuario_id`) turned out fragile and produced its own confusing bugs (see the `aliados.tipo` investigation above, and the wrong-Neon-project/session-mixup debugging earlier the same day). Federico asked for a systemic fix instead of another one-off patch: any admin should be able to view/test any aliado-scoped panel (today: `/taller`'s `PublicarServicioTaller`, the Panel de Inspecciones) without creating or linking any real or fake `aliados` row.

**Two modes, both read-only, both admin-gated:**
- **Vista previa** — zero DB involvement, returns static mock data (`estadoPublicacionVistaPrevia()` in `talleres-desempeno.service.ts`) so an admin with no aliado at all can see the UI populated.
- **Ver como [Aliado real]** — an admin picks a real, `aprobado` aliado from a dropdown; the panel then reads (never writes) that aliado's real data.

**The security boundary is structural, not cosmetic: only GET/read endpoints ever accept impersonation.** The shared resolver `resolverAliadoParaLectura(user, aliadoIdSolicitado)` (`src/services/inspeccion.service.ts`) is called exclusively from `GET` handlers (`app/api/v1/inspecciones/contexto/route.ts`, `app/api/inspector/cit/route.ts`, `app/api/v1/talleres/servicio-publicado/route.ts`). Every `PUT`/`POST` (mutating) handler on those same routes still uses the original strict, ownership-only resolvers (`resolverAliadoDeUsuario`/`resolverAliadoIdDelUsuario` — `WHERE usuario_id = $1`, no `aliadoId` parameter accepted at all) — an admin in "ver como" mode structurally cannot approve an inspection, report a discrepancy, or publish/edit a taller's service listing under someone else's identity, because the code path that would do that never reads the impersonation parameter in the first place. `POST /api/inspector/cit`'s handler has an inline comment spelling this out explicitly (approving/reporting a discrepancy signs a real acta; an admin in "ver como" can never attribute that action to an aliado that isn't their own).

`resolverAliadoParaLectura()`'s branches: `rol === 'aliado'` → always the caller's own aliado (`modo: 'propio'`), ignores any `aliadoIdSolicitado`. `rol === 'admin'` with an `aliadoIdSolicitado` → looks up that aliado by id (`estado = 'aprobado'` only), `modo: 'ver_como'`. `rol === 'admin'` with no `aliadoIdSolicitado` → `aliado: null, modo: 'vista_previa'`. Any other role (`inspector`) → always `modo: 'propio'`, `aliado: null` (unaffected by this feature).

**Client-side state is entirely stateless** — a single URL query param `?verComoAliado=<aliadoId>`, read by a shared hook `useVerComoAliado()` (`lib/admin-view-as.ts`, wraps `useSearchParams`). No new localStorage, no new React context. `SelectorVerComoAliado.tsx` (admin-only dropdown, fetches the already-existing `GET /api/v1/admin/aliados?estado=aprobado` — no new backend endpoint needed for the picker itself) writes to this param via `router.push`; any panel reading the hook reacts automatically. `AdminViewAsBanner.tsx` renders an unmistakable amber banner ("Vista previa..." or "Viendo como: [nombre] (Taller Aliado real) — modo solo lectura...") whenever `modo !== 'propio'`, and both wired-up panels (`PublicarServicioTaller.tsx`, `inspecciones.tsx`) additionally disable their mutating actions (publish/edit button, aprobar/discrepancia buttons) client-side when not in `'propio'` mode — a UX courtesy on top of the real server-side boundary above, not the actual security mechanism.

**Extending this to a future aliado-scoped panel** means: (1) have its `GET` handler call `resolverAliadoParaLectura(user, verComoAliado)` instead of the strict resolver, forward `?verComoAliado=` from the query string, and return `modoVista` in the response; (2) keep its `PUT`/`POST` handlers untouched (strict resolver only); (3) on the client, read `useVerComoAliado()`, append it to the `GET` fetch, and render `<SelectorVerComoAliado />` + `<AdminViewAsBanner modo={...} />`.

### Remito de Embalaje y Despacho (Fase 6b, CIT Completo) — built 2026-07-12/13

Formalizes, with a signed PDF, the packaging work order a seller gives the Taller Aliado once a CIT Completo sale's saldo is confirmed (`escrow_transacciones.estado = 'FONDOS_RETENIDOS'`), and — in a second step — serves as the Taller's signed proof that it actually packaged and dispatched the bike. Two distinct acts on one `remitos` row: `generarRemito()` (seller, explicit action — the only way the Taller finds out it needs to pack the bike) → `confirmarDespachoRemito()` (Taller, signs with its own `wallet_address`, same mechanism as the 20-point inspection acta) → triggers the Fee de Logística liquidation.

**Where it lives in the money flow — a real correction made while building this:** while investigating, found that `registrarLiquidacionAliadoFeeLogistica()`/`FeeExito()` (assumed missing per this file's own prior text and per Federico's own recollection) had actually **already been built**, in commit `233922f` (2026-07-11, the real-titularidad-transfer work) as part of `confirmarEntregaCitCompleto()` — the buyer's final "confirm receipt" endpoint, which closes `FONDOS_RETENIDOS → COMPLETADA`. Both this file's old "Known gap... no endpoint exists" text and `escrow.service.ts`'s own inline `TODO(cierre CIT Completo)` comment were stale and have been corrected. Design decision made explicitly with Federico: `registrarLiquidacionAliadoFeeLogistica()` was moved OUT of `confirmarEntregaCitCompleto()` and into `confirmarDespachoRemito()` instead — the Taller collects the embalaje fee the moment it finishes its own work (Despacho a Logística), not days later when a third party (the buyer) confirms final delivery. Same principle already documented for the Fee de Verificación (paid at checklist-seal time, regardless of whether the sale later falls through). `confirmarEntregaCitCompleto()` still liquidates the Fee de Éxito (correctly contingent on the sale actually completing).

**No new state on `escrow_transacciones.estado`/`marketplace_publicaciones.estado`:** deliberately avoided adding another enum value to either (both are real Postgres enums subject to the two-deploy `ADD VALUE` rule elsewhere in this file). `remitos.estado` (`GENERADO`/`DESPACHADO`, a brand-new enum type, safe to create+use in the same migration) is the sole source of truth for this sub-tramo, read directly by the frontend via `MiPublicacion.transaccion.remito`/`MiCompra.remito`.

**Schema:** `remitos` (correlative `numero` via a new Postgres `SEQUENCE`, `remitos_numero_seq` — the first sequence in this schema; formatted `REM-{year}-{padded}` in `remito.service.ts`, not in SQL) + the full firma-completa column set mirrored from `inspecciones_fisicas` (`firma_algoritmo`/`valor`/`certificado`/`cert_serie`/`cert_fingerprint`/`modo`, not just a hash — this file's first draft of the migration only had `firma_hash` and had to be corrected before implementing the service layer). Three new nullable `TIMESTAMPTZ` columns on `escrow_transacciones`: `saldo_confirmado_en` (stamped in the webhook's `SALDO_PENDIENTE → FONDOS_RETENIDOS` branch — deliberately NOT reusing `updated_at`, which any later write, including the reminder timestamps below, would overwrite), `remito_recordatorio_in_app_en`, `remito_recordatorio_email_en` (both still unused — belong to the reminder worker, not yet built).

**Two-deploy split, same rule as documented elsewhere in this file:** `20260712000001_create_remitos.sql` (the table/sequence/columns above, all safe) shipped separately from `20260712000002_notif_tipo_remitos.sql` (`ALTER TYPE notif_tipo ADD VALUE 'REMITO_GENERADO'/'REMITO_RECORDATORIO'/'REMITO_DESPACHADO'`) — the second migration has to merge, deploy, and confirm `ready` in production on its own before any PR referencing those values (the notification-dispatch code, still unbuilt) can merge.

**Signing:** `acta-firma.service.ts` gained a new exported `firmarCanonico(canonico: string)` — the same PKCS#12/DEV credential and Web Crypto RSASSA-PKCS1 algorithm `firmarActa()` already used, generalized to sign an arbitrary canonical string instead of only an `ActaPayload`. `firmarActa()` itself now delegates to it (additive refactor, zero behavior change for its existing callers). The Remito PDF itself is additionally signed with the Autoridad Certificadora's PKCS#7 detached signature (same `firma.service.ts`/`crearSignerRodaid()` mechanism as the CIT certificate) — two independent signature layers: one proves RODAID issued the document, the other proves a specific Taller staff member confirmed the physical work.

**Public verification (`GET /api/v1/verificar/remito/[numero]`, `/verificar/remito/[numero]`):** deliberately its own route, not a reuse of `/api/v1/verificar/[serial]` (bike identity has a different shape/veredicto than "is this document genuine") — but does reuse `verificacion.service.ts`'s IP-based rate limiter (`hashIp`/`chequearRateLimit`, fully generic, no bike-specific coupling) to share the same anti-enumeration budget. Deliberately does NOT write to `logs_verificaciones` — that table/its `tipo_busqueda` column is shaped for the bike veredicto, not a remito event; forcing it in would have meant either a schema change (not asked for) or misusing the existing values.

**Download auth gotcha, worth remembering for any future authenticated-PDF-download UI:** `GET /api/v1/remitos/[numero]/pdf` requires `Authorization: Bearer` (via `requireAuth`), so a plain `<a href>` would NOT authenticate — the browser doesn't attach that header on normal navigation. `lib/remitos.ts::descargarRemitoPdf()` fetches the blob via `authedFetch` and triggers the download from an in-memory object URL instead. (Investigated whether the existing CIT certificate download, `/api/v1/cit/[id]/certificado`, had already solved this and could be copied — it hasn't: a full-repo grep found **zero frontend callers of that route at all**, so there was no existing pattern to follow here.)

**Known gap, not fixed in this pass, deliberately deferred:** `confirmarEntregaCitCompleto()` (the buyer's final "confirm receipt" endpoint that actually closes a CIT Completo sale, referenced above) has **zero frontend callers** — confirmed via a full-repo grep for `confirmar-entrega-cit-completo` across `components/`. A CIT Completo transaction that reaches `FONDOS_RETENIDOS` and gets a Remito `DESPACHADO` has no UI path for the buyer to ever confirm final delivery, release the funds, or transfer titularidad — it would sit there indefinitely with no frontend way to close it. Same category as the other documented "backend exists, no frontend caller" gaps in this file (e.g. the `pagoPendiente`/denuncia resume-later gap). Not built as part of the Remito work — it's a separate, larger feature (a real "confirm I received my bike" UI flow) that needs its own pass.

### Auth / admin RBAC

There is no root `middleware.ts`. Admin route protection is enforced at the edge by `netlify/edge-functions/auth-admin.ts`, bound in `netlify.toml` via `[[edge_functions]]` to `path = "/api/v1/admin/*"` — it runs before any redirect or the origin function (defense in depth), in addition to in-route `requireUser`/`requireRole` checks.

### `app/admin/page.tsx` ("Dashboard de Administración") is a distinct panel from `app/admin/gov/page.tsx` ("Panel Gubernamental")

Two completely separate admin surfaces exist and are easy to conflate by name alone: `app/admin/gov/page.tsx` ("RODAID · Panel Gubernamental") is gated by a single shared static token (`X-Gov-Token` vs `GOV_API_TOKEN`), unrelated to any logged-in user. `app/admin/page.tsx` / `components/rodaid/admin-dashboard.tsx` ("Dashboard de Administración", header text "Operaciones · Administración") is a different, older panel (Hito 19) gated by three layers: (1) `requireRole('admin')` on the user's own JWT session (`usuarios.rol = 'admin'`), (2) obligatory MFA (TOTP) step-up with its own short-lived token, and (3) a sub-role (`admin_perfiles.admin_rol`: `superadmin` / `auditor` / `soporte`) with its own permission matrix (`lib/admin-panel.ts`'s `MATRIZ`) that decides which of the 5 tabs (Integridad, Moderación, Analítica, Identidades, Bitácora) are visible. Confirmed live in production (2026-07-10) that `RODAID_SUPERADMIN_EMAILS` is **not set** — meaning `rolPorDefecto()` falls through to `soporte` for every admin the first time their `admin_perfiles` row is provisioned, regardless of email. This does not by itself explain a visibility difference between two admin accounts, since all three sub-roles include `analitica:ver` — but it's a real gap worth closing if the intent was ever for specific emails to get `superadmin` automatically.

### Fixed 2026-07-10: no real logout in the Admin Dashboard or the Taller Aliado panel, only "Cerrar MFA"

Found while investigating a Federico report ("no encuentro un botón de Cerrar sesión en el panel de administración, solo vi Cerrar · MFA"). A full-repo sweep of every role-gated panel under `components/rodaid/` found the real logout mechanism (`clearSession()` + redirect, from `lib/session.ts`) existed in exactly one place before this fix: `garaje-digital.tsx` (the citizen's own "Mi Garaje" dashboard). Every other panel had none:

- `app/admin/page.tsx` / `components/rodaid/admin-dashboard.tsx` (Dashboard de Administración): the only header button was `"Cerrar MFA"`, wired to `clearStepUp()` — which only removes the admin's MFA step-up JWT from `localStorage` and returns to the MFA verification gate. It never called `clearSession()` and never hit `POST /api/v1/auth/logout` (which exists, revokes the RefreshToken server-side, but had zero callers anywhere in the frontend before this fix).
- `app/taller/page.tsx` (Panel del Taller Aliado): had no logout button of any kind.

Fixed by (1) renaming the admin panel's existing button to `"Re-verificar MFA"` so it can't be mistaken for a real logout, and (2) adding a genuine `"Cerrar sesión"` button (same `clearSession()` + redirect-to-`/` mechanism as `garaje-digital.tsx`) to both `admin-dashboard.tsx` and `app/taller/page.tsx`.

**Causa probable, no confirmada**, surfaced during the same investigation: Federico separately described a "Monitor de Integridad" (the Integridad tab in the Admin Dashboard) error that appeared to resolve itself without any action he could pin down. `ACCESS_TOKEN_TTL` is `'15m'` (`lib/auth.ts`), and the *separate* admin MFA step-up token defaults to a 30-minute TTL (`stepUpTtlSeg()`, `lib/admin-panel.ts`) with no auto-renewal. `authedFetch()` (`lib/session.ts`) reacts to *any* 401 — including one caused by an expired MFA step-up header, unrelated to the base session token — by first refreshing the base AccessToken (a no-op fix for this specific cause) and, if still 401, calling `clearSession()` (wiping the entire base session, not just the admin step-up) and starting a fresh one. Mechanically, this could produce exactly the symptom described: an error that "resolves on its own" after a page reload sends the user back through the MFA gate. **The timeline was not confirmed** — Federico could not recall whether a page reload happened or how much time elapsed before the tab worked again — so treat this as a plausible mechanism, not a confirmed root cause, until it recurs and the timeline can be captured.

**Adjacent finding, not urgent, noted for later prolijidad:** the same sweep checked whether `/admin/pagos` (`pagos-dashboard.tsx`) and `/admin/aliados` (`aliados-admin.tsx`) had the equivalent gap. Confirmed their backend endpoints ARE genuinely protected — `GET /api/pagos/resumen` (`requireRole('admin','aliado')`), `POST /api/v1/admin/pagos/liquidar` (`requireStaff(req,'admin')`), `GET /api/v1/admin/aliados` and `POST /api/v1/admin/aliados/[id]/aprobar` (both `requireStaff(req,'admin')`) — all backed by a cryptographically-verified JWT or, for `requireAdmin`'s system-token fallback, a real comparison against `RODAID_ADMIN_TOKEN` that fails closed if the env var is missing. Neither component has a client-side role/session guard (unlike `app/taller/page.tsx`'s new `getSession()` check), so an unauthorized visitor can mount the page and see a loading state before the backend rejects the request — a defense-in-depth/UX gap, not a data exposure. Left unfixed, deliberately deferred as low-priority polish.

### Security incident: demo-session allowed unauthenticated privilege escalation to admin (2026-07-08/09)

**What was found.** `POST /api/v1/auth/demo-session` had zero authentication and read `rol` straight from the request body: `rolPedido && ROLES_DEMO.has(rolPedido) ? rolPedido : 'ciclista'`, where `ROLES_DEMO` included `admin`. Any unauthenticated caller could `POST {"rol":"admin"}` and receive a real, validly-signed AccessToken with `rol: admin` — a complete bypass of the assumption that only `contactoarribaeleste@gmail.com` and `federicodegeaceo@rodaid.net` hold admin privileges. That token satisfied `requireRole('admin')` / `requireStaff(req, 'admin')` everywhere, including every route under `/api/v1/admin/*`.

**How it was found.** While building and manually verifying the session-invalidation feature (the `sesion_invalidada_desde` watermark, see below), the verification script itself needed an admin Bearer token to call the new admin endpoint — it got one by calling `demo-session` with `{"rol":"admin"}`, which is when it became apparent this worked with no login at all.

**Confirmed exposure window:** 2026-06-30 to 2026-07-09 (~10 days), **18 illegitimate admin accounts** generated (`usuarios` rows with `email LIKE 'demo-%@rodaid.test'` and `rol = 'admin'`, counted via a read-only query against the production Neon branch).

**Two fix attempts — the first shipped broken, the second is what's live:**
1. `705e839` gated the endpoint on `process.env.CONTEXT === 'production'`. This looked right (`CONTEXT` is the value Netlify's own deploy API reports, confirmed via `getDeploy` all session as `"context": "production"`) but **does not propagate to this route's runtime**: Next.js App Router routes run through `@netlify/plugin-nextjs`, a different runtime path than raw `netlify/functions/*.mts` (which do successfully read `process.env.URL`/`RODAID_ADMIN_TOKEN`). Confirmed broken by exploiting it live in production immediately after deploy `6a4efa3a5838480008e49d81` went `ready`: `POST https://rodaid.net/api/v1/auth/demo-session {"rol":"admin"}` still returned `200`, not `403` (the role itself was correctly forced to `'ciclista'` by a separate, working part of the same fix — only the production-gate layer was silently inert).
2. `67c3232` replaced the check with `req.headers.get('host')` against the site's real domains (`rodaid.net`, `www.betarodaid.net`, `rodaid.netlify.app`, `main--rodaid.netlify.app`, confirmed via `netlify api getSite`). Verified empirically before trusting it: a request to `https://rodaid.net` with a spoofed `Host` header (TLS SNI still genuinely targeting rodaid.net) got a `404` **from Netlify's edge**, never reaching application code — proving Netlify validates `Host` for routing and a request that reaches this route necessarily carried a real, registered `Host`. Confirmed fixed live in production after deploy `6a4f01eac78e7500089bf32f`: same exploit attempt returned `403 {"error":"DEMO_DESHABILITADO"}`.

**Permanent rule:** never read a role, permission, or any other authorization-affecting field from the body of an endpoint that has no authentication check. If an endpoint is meant to be unauthenticated (demo/onboarding flows, public forms), any privilege-bearing field it might otherwise accept from the caller must be hardcoded server-side instead, with no path for the request body to influence it.

**Related cleanup (2026-07-09):** while investigating the incident above, found and deleted three more unauthenticated debug/bootstrap endpoints that had no reason to still exist in a production system:
- `netlify/functions/set-admin.mts` and `app/api/set-admin/route.ts` — two duplicate, unauthenticated endpoints (one a raw Netlify Function, one a Next.js route) that ran a hardcoded `UPDATE usuarios SET rol = 'admin' WHERE lower(email) = 'federicodegeaceo@rodaid.net'` via a raw fetch to Neon's HTTP SQL API (bypassing `getPool()`/`@netlify/database` entirely, using `DATABASE_URL`/`NETLIFY_DB_URL` directly). One-time admin-bootstrap scripts that outlived their purpose — same underlying pattern as the demo-session incident (privileged DB mutation behind an endpoint with zero auth), just not exploitable for arbitrary accounts since the target email was hardcoded.
- `app/api/debug-db/route.ts` — unauthenticated endpoint that leaked `NETLIFY_DB_URL`'s parsed hostname/protocol/pathname and whether user/password were set (not the values themselves, but still internal infra detail with no auth gate).

None of the three were the cause of two `usuarios` rows disappearing from production overnight during this same investigation — that was ruled out separately (no `DELETE` targeting `usuarios` exists anywhere in this codebase's routes, services, or migrations, and Postgres FK `ON DELETE CASCADE` only cascades parent→child, never child→parent, so no other table's row deletion could have removed a `usuarios` row). All three were deleted outright rather than gated behind `requireAdmin`, since none had any ongoing purpose once the admin accounts they bootstrapped already exist.

### Critical finding, undocumented until now (2026-07-11): widespread lowercase/uppercase mismatch on `cits.estado` — likely breaks `tieneCitActivo()` and others in production, needs its own investigation session

Found while designing the real-titularidad-transfer work (see the transfer-of-ownership section above). `cit_estado` (the enum backing `cits.estado`, defined in `netlify/database/migrations/20260618121000_create_cit_tablas.sql`) only has **uppercase** values (`'PENDIENTE_VALIDACION'`, `'PROCESANDO_CRUCE'`, `'ANOMALIA_DETECTADA'`, `'ACTIVO'`, `'VENCIDO'`, `'RECHAZADO'`, `'REVOCADO'`) — confirmed directly from the migration SQL. The only function that actually activates a CIT, `validarCIT()` in `cit.service.ts`, correctly writes `estado = 'ACTIVO'` (uppercase). Postgres enum comparison is case-sensitive/exact-match, so any query comparing `estado` against `'activo'` (lowercase) can never match a real row.

**A grep across the whole repo for `estado = 'activo'` (lowercase) found it in well over a dozen places**, several of them live, production-facing code paths — not confirmed exhaustively, but confirmed individually for each file listed:

**Reads (silently return zero rows / always false, no error — the dangerous kind, nothing crashes to reveal it):**
- `src/services/cit.service.ts:1059` — `tieneCitActivo()`, exported and presumably meant to answer "does this user have an active CIT."
- `src/services/garaje.service.ts:172, 653` — the `cit_activo`/`activo_vigente` computed columns feeding the actual "Mi Garaje" dashboard.
- `src/services/blockchain.service.ts:389` — `anclarPendientes()`, the function `POST /api/v1/admin/blockchain/anclar` calls, which the live `bfa-anclaje-worker.mts` cron invokes every 5 minutes. Also checks `c.bfa_estado = 'pendiente'` lowercase against `cit_bfa_estado`, same mismatch, same enum-uppercase-only problem. If this is really broken, the BFA anchoring retry cron has been running every 5 minutes for a while now while silently finding zero CITs to retry, regardless of how many are actually stuck pending.
- `app/api/v1/bicicletas/route.ts:58, 73` — the live "list my bikes" endpoint.
- `app/api/v1/bicicletas/[id]/verificar/route.ts:70`
- `app/api/v1/cit/[id]/certificado/route.ts:48`
- `app/api/v1/cit/[id]/credencial/route.ts:41`
- `app/api/v1/gov/certificado/route.ts:45`
- `app/api/v1/gov/verificar/route.ts:59`
- `app/api/v1/marketplace/publicar/route.ts:195`
- `app/api/v1/usuarios/[id]/estadisticas/route.ts:12`
- `app/api/v1/buscar/route.ts:53, 62` — already documented elsewhere in this file as an orphaned prototype route (item 13), not live-user-facing, but same bug present.
- `src/services/stravaWebhookService.ts:258` — already documented elsewhere in this file as fully orphaned/dead code (never imported anywhere), so not a live-impact instance, just the same pattern repeated.
- `app/api/v1/bicicletas/[id]/transferir/route.ts:64` — **fixed today** as part of the real-titularidad-transfer work (this file was being rewritten anyway); no longer relevant as an open instance, listed here only for completeness of what the sweep found.

**Writes — more concerning, because these attempt to write an invalid enum literal, not just fail to match one:**
- `src/services/ministerio.service.ts:490` — `UPDATE cits SET estado = 'activo', ...` (unblocking a bici after it was blocked, part of the Ministerio de Seguridad cross-reference flow).
- `src/services/validation.service.ts:440` — `UPDATE cits SET estado = 'activo', ...` (part of the validation pipeline).

**What's NOT yet confirmed, and is exactly the point of a dedicated session:** whether these writes actually throw at runtime (an invalid literal against a real Postgres enum should error immediately — `invalid input value for enum cit_estado`), which would mean these code paths have effectively never completed successfully in production and would have left an error-log trail; or whether something about the actual column/constraint setup is more permissive than the migration SQL alone suggests. Also unconfirmed: whether this genuinely explains any specific bug Federico has reported (e.g., a bici not showing as verified when it should), or whether some other code path compensates and the practical impact is smaller than the sweep suggests. **Do not assume the worst-case interpretation above is correct without re-verifying against live behavior first** — this section documents what the grep and source-reading found, not a confirmed root-cause diagnosis.

Deliberately not touched in this pass beyond the one file already being rewritten for an unrelated reason (`/transferir`) — this is a candidate for its own dedicated investigation-and-fix session, given the number of files involved and that it may be silently affecting functions already live in production.

**Update, resolved 2026-07-11/12 — see the two sections directly below.** The dedicated session this note asked for happened: it found `cit_estado` isn't uppercase-only after all (`20260701000001_add_bloqueado_cit_estado.sql` and `20260701000002_fix_cit_estado_enum.sql` added `'bloqueado'`/`'activo'`/`'pendiente'`/`'rechazado'` in lowercase to the *same* enum on 2026-07-01) — the real bug is that **two casing conventions coexist in one enum**, not that the lowercase writes are universally invalid. `/transferir`'s vigencia check was fixed (see the transfer-of-titularidad work above). Separately, `cits.bfa_estado` turned out to have the exact same class of bug (`blockchain.service.ts` writing lowercase against the uppercase-only `cit_bfa_estado` enum, meaning BFA anchoring never wrote successfully since 2026-06-18) — fixed 2026-07-12. A full-schema sweep for a fourth occurrence of this pattern came back clean.

### Fixed 2026-07-12: `cits.bfa_estado` casing bug + retirement of the dead BFA "Generación 1"

`blockchain.service.ts` (the ethers.js-direct anchoring mechanism, wired to the live approval pipeline via `anclarCITEnSegundoPlano()`/`bfa-anclaje-worker.mts`) wrote/read `bfa_estado` in **lowercase** (`'anclado'`/`'pendiente'`/`'anclando'`/`'error'`) against `cit_bfa_estado`, an enum that only ever accepted **uppercase** (`NO_INICIADA`/`PENDIENTE`/`ACUNADO`/`ERROR`/`FALLIDO`) — confirmed by a failed deploy (`22P02: invalid input value for enum cit_bfa_estado`) and, empirically, by a read-only production query Federico ran: all 3 real CITs sat at `NO_INICIADA` with `bfa_tx_hash`/`bfa_intentos` at zero. **BFA anchoring never wrote successfully a single time since `cits` was recreated on 2026-06-18** — not even in the honest STUB/simulated sense documented in the item below; `reclamarAnclaje()`'s very first `UPDATE` threw before ever reaching the STUB-hash computation, silently swallowed by `anclarCITEnSegundoPlano()`'s `.catch(console.error)`. Fixed: all 5 read/write sites in `blockchain.service.ts` moved to uppercase, `'anclando'` (no equivalent value in the real enum) replaced by reusing `'PENDIENTE'` (this system has no real concurrency between workers — the `FOR UPDATE` row lock already prevents double-anchoring), and `anclarPendientes()`'s sweep widened to pick up `NO_INICIADA` as well as `PENDIENTE` (a CIT whose post-approval fire-and-forget trigger failed before reaching the claim step was previously orphaned forever). Same fix applied to the 5 consumers that inherited the identical bug, including the STUB/ONCHAIN honesty-UI work from the same day (`validation.service.ts`, `verificacion.service.ts`, `webhooks-ecosistema.service.ts`, `credenciales-vc.service.ts`, `pdf.service.ts`, `garaje-digital.tsx`, both `gov/` routes) and `src/types/database.ts`'s stale `BfaAnclajeEstado` type.

The same investigation confirmed **two full, unrelated generations of BFA-anchoring mechanism coexisted** in this repo: Generación 2 (`blockchain.service.ts` + `bfa-anclaje-worker.mts`, ethers.js direct RPC + deployed contract, born 2026-06-16, wired to the live pipeline) and a Generación 1 (`lib/bfa.ts`'s gateway-HTTP abstraction, born 2026-06-06/07 as "Módulo 4: Configuración Final", with two triggers of its own — the manual `POST /api/v1/cit/[id]/acunar` endpoint and the hourly `cron-acunacion-bfa.mts` sweep) that was never deleted when Generación 2 superseded it ten days later. Confirmed via full-repo grep that Generación 1's manual endpoint and its `GET /api/v1/admin/cit/mints/fallidos` companion had **zero frontend callers** (no admin-panel button anywhere) and zero other backend callers — genuinely dead, not just redundant. Retired 2026-07-12: `cron-acunacion-bfa.mts`, both routes, and the four now-orphaned `cit.service.ts` functions (`prepararAcunacionBFA` — which had *zero* callers anywhere, even more dead than the rest — `registrarAcunacionBFA`, `acunarCITEnBFA`, and the `acunarCIT` alias) deleted outright, along with `cit.service.ts`'s now-dead `@/lib/bfa` import. `lib/bfa.ts` itself, `obtenerEstadoMint()` (a live, read-only status endpoint independent of which generation writes the columns), and `transferencia-dominio.service.ts` (a genuinely distinct purpose — anchoring ownership-transfer events to `cit_transferencias`, not CIT identity — that legitimately still uses `lib/bfa.ts`'s engine) were deliberately left untouched.

### RESOLVED 2026-07-12: full-schema sweep for the `cits.estado`/`cits.bfa_estado` casing pattern — no fourth case found

After fixing `cits.bfa_estado` (see "Fixed 2026-07-12" section above) Federico asked for a systematic sweep of the *entire* schema — every enum type and every `VARCHAR`/`TEXT` + `CHECK IN (...)` "enum-like" column across all 65 migrations — to confirm whether the casing-mismatch pattern (real pipeline writes one convention, some older/orphaned code compares against another) recurs anywhere else. It doesn't.

**Method:** enumerated all 16 `CREATE TYPE ... AS ENUM` definitions and 18 `CHECK IN (...)` columns from the migration files (using the *last* migration that defines/recreates each, not the first — in case of a `cits`-style `DROP TABLE`), then grepped `src/services`, `app/api`, `netlify/functions`, and `lib/` for every read/write of each column and checked exact-casing agreement against the real valid-value set.

**Why only two tables were ever candidates:** the failure mode needs a `DROP TABLE ... CASCADE` recreation *plus* code written before that recreation that never got revisited. A repo-wide grep for `DROP TABLE` (with or without `CASCADE`) found exactly two occurrences in the whole migration history: `cits`+`bicicletas` (`20260618121000_create_cit_tablas.sql`, the one that caused both known bugs) and `tenants` (`20260706000003_multi_tenant_rls.sql`). `tenants.tipo` (a plain `TEXT` + `CHECK`, not a real Postgres enum) checked out clean — its only consumer (`lib/tenant.ts`) was written in the same session that recreated the table, so there was never a stale/orphaned reader to diverge. No other table in the schema was ever dropped and recreated — everything else was only ever extended additively (`ALTER TYPE ... ADD VALUE`, `ALTER TABLE ... ADD COLUMN`), always by code that already knew the current convention.

**Everything else checked (all confirmed correctly cased, no exceptions found):** `escrow_transacciones.estado`, `mp_pagos.estado`, `marketplace_publicaciones.estado`, `pagos_liquidaciones.tipo`/`.estado`, `tasas_cit.estado`, `cola_validaciones.estado`, `usuarios.rol`/`.estado`, `aliados.estado`/`.tipo`, `inspecciones_fisicas.resultado`, `seguridad_alertas.estado`/`.severidad`, `analitica_discrepancias.tipo`, `notificaciones.tipo`, `denuncias_terceros.estado`, `dispositivos_iot.estado`, `developer_apps.estado`/`.entorno`, `ecosystem_webhooks.estado`, `denuncias_mpf.estado`/`.ministerio_estado`, `cit_transferencias.motivo`/`.bfa_estado`, `cits.bfa_modo`, `salidas_grupales.estado`/`.nivel`, `valoraciones.tipo`, `noticias.tipo`, `parametros_pricing_cit.tipo`, `talleres_servicios.servicio`, `bicicletas.talle_cuadro`/`.rodado`, `logs_verificaciones.tipo_busqueda`.

**Conclusion, stated with the same confidence either way was going to be reported:** the three already-known instances (`cits.estado`, `cits.bfa_estado`, and the orphaned BFA Generación 1 that inherited `cits.estado`'s old uppercase convention) are the *only* occurrences of this pattern in the codebase as of 2026-07-12. This closes the investigation opened above — no dedicated follow-up session needed for this specific class of bug unless a *new* table gets dropped and recreated in the future without a matching code audit.

### RESOLVED 2026-07-12: "Rodaidbeta.app" external-origin hypothesis for the Generación 1 BFA code — no evidence found, discarded

Federico floated a hypothesis for *why* `lib/bfa.ts`/`cit.service.ts::acunarCITEnBFA()`/`cron-acunacion-bfa.mts` (the retired Generación 1 above) ended up out of sync: that this code might have originated in an earlier prototype/demo he'd shared, "Rodaidbeta.app," and been copy-pasted or adapted into this repo without fully updating it to this repo's real schema. Investigated directly rather than assumed either way:

- **Literal search** (`rodaidbeta`, `beta.app`, and variants, case-insensitive) across all code, comments, filenames in the full git history, and commit messages: **zero matches**. The only "beta" hit anywhere in commit history is `67c3232` (2026-07-08, the demo-session security fix), and it refers to `www.betarodaid.net` — RODAID's own already-documented staging subdomain, not an external project; a substring coincidence, not a real lead.
- **git log with author or the three files:** all three were authored by **`netlify[bot]`**, not a human, as part of the same automated session-labeled commit flow this repo has used since its earliest RODAID-specific work (`4fc0686`, 2026-06-06, the Marketplace module). `lib/bfa.ts` and `cron-acunacion-bfa.mts` were both born in the exact same commit, `7ca971e`, 2026-06-06 22:46:39 UTC. `cit.service.ts::acunarCITEnBFA()` specifically (confirmed with `git log -S"acunarCITEnBFA"`, not just the file's overall creation date) was added in `a6423b6`, 2026-06-07 04:16:58 UTC. **All three commits share the identical session ID `6a248be413060e5e5072e309`** ("Session 3" and "Session 1" of the same run) — conclusive proof the three files were generated together, in one working session against *this* repo, not assembled from separate/external sources at different times.
- **Style comparison:** identical conventions to the rest of the codebase written in this same era and after — the `"RODAID — Módulo N: ..."` header-comment format, the numbered `// ── N. nombreFuncion ──` section markers (same convention `cit.service.ts`'s still-live `iniciarCIT`/`validarCIT` use today), Spanish-language identifiers throughout, and the same canonical-JSON-then-SHA-256 hashing helper pattern `lib/cit.ts` uses for the CIT fingerprint (`lib/bfa.ts`'s own comment says it's a deliberate reimplementation, not a coincidence, "para que la capa BFA no dependa de nada más que `node:crypto`"). No stylistic discontinuity anywhere.
- **README/`package.json`/config:** the only non-RODAID project reference anywhere in the repo is the original Vercel "Vibe Coding Platform" scaffold (`package.json` name `vibe-coding-agent`, already documented at the top of this file as the repo's literal, known origin) — nothing pointing to a second, different external project.

**Conclusion: no evidence of an external origin. Discarded.** The sync bugs are fully explained by what the 2026-07-11/12 investigations already established — Generación 1 (this session, `6a248be413060e5e5072e309`, 2026-06-06/07) and Generación 2 (`blockchain.service.ts`+`bfa-anclaje-worker.mts`, a separate session ten days later, `6a30cb434d2da08aeec04947`, 2026-06-16) were both written directly inside this repo by the same kind of automated agent-session workflow, and nobody deleted Generación 1 when Generación 2 superseded it for the live pipeline. This closes the investigation chain opened by the `/transferir` regression on 2026-07-11.

### Known gap, low priority: `inspector_licencias.estado` is a prepared column with zero functionality built on top of it

Found during the schema sweep above, unrelated to the casing pattern (this one isn't a mismatch — it's simply unused). `netlify/database/migrations/20260617180000_create_admin_dashboard.sql:136-143` creates `inspector_licencias` (inspector license number, `estado` — `'activa'`/`'suspendida'`/`'vencida'` — and expiration date), clearly meant to back an inspector-license-management feature. A full-repo grep found **zero** code anywhere (`inspeccion.service.ts`, the admin panel, any route) that reads or writes this table — it's not a casing bug, there's simply no feature built on top of the table yet. No urgency; noted here for whenever inspector license management gets picked up, so that work starts from "the table already exists, empty" instead of rediscovering it from scratch.

### Background jobs

`netlify/functions/*.mts` — scheduled/background work: BFA (Blockchain Federal Argentina) anchoring worker and cron, CIT pipeline cron, IoT anonymization worker, validation worker, public CIT verification function.

### DEV/STUB fallback pattern

Most institutional integrations (BFA blockchain anchoring, PDF certificate signing authority, acta PKCS12 signing, Web Push VAPID keys, MxM/Mendoza gov OIDC login, Ministerio de Seguridad mTLS cross-reference) are designed to run in a deterministic **simulated/DEV mode** when their env vars are absent, so the full flow is exercisable in preview without real government/blockchain credentials. Read the relevant block in `.env.example` before concluding a feature "requires production secrets to test" — it usually doesn't.

### Privacy-by-design analytics

Location-based features (security heatmap, personal "Garaje Digital" heatmap) clip coordinates to a coarse grid (`ANALITICA_GRID_DEG`, ~500m cells) before persisting, and only surface aggregates above a k-anonymity threshold — never raw points. Preserve this clipping if touching those code paths.

### Mobile

`android/` and `ios/` are Capacitor shells (`capacitor.config.ts`, appId `net.rodaid.app`). `server.url` points at `https://rodaid.net` — the native apps are thin WebView wrappers loading the live deployment, not bundlers of a local static `webDir` build.

### UI

`components/ui/*` — shadcn/ui primitives (`new-york` style, see `components.json`). `components/rodaid/*` — the actual product UI (marketing site sections, dashboards, forms).
